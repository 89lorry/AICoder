# AICoder - Multi-Agent Code Generation System with MCP Protocol
# Updated for True Model Context Protocol Implementation

# ============================================================================
# CORE DEPENDENCIES (Required)
# ============================================================================

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0

# Web UI framework
gradio>=4.0.0

# Environment configuration
python-dotenv>=1.0.0

# HTTP requests
requests>=2.31.0

# Google Generative AI (Gemini API) - Primary LLM provider
google-generativeai>=0.3.0

# ============================================================================
# OPTIONAL DEPENDENCIES (For advanced features)
# ============================================================================

# LangChain for agent memory (ENABLED by default - ENABLE_MEMORY=true in .env)
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.20

# LangChain LLM integrations
langchain-google-genai>=1.0.0  # For Google Gemini integration
# langchain-openai>=0.0.5  # Uncomment if using OpenAI
# langchain-anthropic>=0.1.0  # Uncomment if using Anthropic

# Alternative LLM providers (optional)
# openai>=1.0.0
# anthropic>=0.7.0

# Memory backends (optional - only needed if using LangChain memory)
# chromadb>=0.4.0
# faiss-cpu>=1.7.4; platform_system == "Linux"

# Web framework (optional - Flask not currently used)
# flask>=2.3.0

# ============================================================================
# MCP PROTOCOL NOTES
# ============================================================================
# This project implements true Model Context Protocol (MCP) using:
# - JSON-RPC 2.0 over stdio
# - Process-isolated agent servers
# - Standards-compliant MCP specification
#
# All MCP components are implemented in pure Python using:
# - asyncio (built-in)
# - json (built-in)
# - subprocess (built-in)
#
# No external MCP SDK is required!

# ============================================================================
# INSTALLATION
# ============================================================================
# Install required dependencies:
#   pip install -r requirements.txt
#
# For development with all optional features:
#   pip install -r requirements.txt
#   # Then uncomment and install optional dependencies above
